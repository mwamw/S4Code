{
  "name": "@s4code/ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript S4Code frontend aligned to the Claude Code framework shape.",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "dev": "bun run src/main.tsx --cwd ..",
    "start": "bun run src/main.tsx --cwd ..",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "smoke": "bun run src/main.tsx --cwd .. --prompt /status && bun run src/main.tsx --cwd .. --prompt /context && bun run src/main.tsx --cwd .. --prompt /help && bun run src/main.tsx --cwd .. --prompt /tasks && bun run src/main.tsx --cwd .. --prompt '/session list' && bun run src/main.tsx --cwd .. --prompt /mcp && bun run src/main.tsx --cwd .. --prompt /quit"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "ink": "^5.1.0",
    "ink-spinner": "^5.0.0",
    "ink-text-input": "^6.0.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/react": "^18.3.12",
    "typescript": "^5.7.2"
  }
}
